`architecture-dark.svg` / `architecture-light.svg` — the theme-aware component diagram
referenced from [`docs/architecture.md`](../docs/architecture.md), rendered via
`<picture>` the same way the main GitHub profile hero banner switches on
`prefers-color-scheme`.

Optional additions if you want to extend this later: a real (anonymized) UI screenshot
or a results chart once evaluation numbers are cleared for disclosure — see the
explicit "not published" boundary in
[`docs/evaluation.md`](../docs/evaluation.md) and
[`docs/limitations.md`](../docs/limitations.md) for why none is included by default.
Any screenshot added here must have no PII, no real client data, and no internal URLs
visible.
